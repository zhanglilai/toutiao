
/* import { mapGetters, mapActions } from 'vuex';

export const mixinData = {
    computed: {
        ...mapGetters([
            'counter'
        ])
       
    },
    methods: {
        ...mapActions([
            'setCounter','setUpdateInfo'
        ]),
    }
} */
